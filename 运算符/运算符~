1.分号 ; 在js代码中不是必需的，同一行的多条命令由 ; 分隔
2.逗号 , 分隔同一条命令中并列的语句
3.空格   在命令间的多少无影响
4.注释
  双左斜杠 // 注释单行命令
  /* 命令 */ 注释多行命令
5.运算(优先级依次递减是: ++ -- ， * / % ， + -)
  var x, y=5; //初始化两个变量x和y，给y赋初始值为5。{x:undefined, y:5}
  x=y+5;     //即x=5+5，x=10
  x=y-5;     //即x=5-5，x=0
  x=y/2;     //即x=5/2，x=2.5
  x=y*2;     //即x=5*2，x=10
  x=y%2;     // 求余 即x=5%2，x=1
  x=--y;     // 递减(先减后运行.而 y++ : 先运行后加) --y等价于y=y-1，此后y=4，然后x=y，即x=4
  x=++y;     // 累加(先加后运行.而 x++ : 先运行后加) ++y等价于y=y+1，此后y=5，然后x=y，即x=5

  x+=2;     //7  x=x+2，即x=5+2
  x-=2;     //5  x=x-2，即x=7-2
  x*=2;     //10 x=x*2，即x=5*2
  x/=2;     //5  x=x/2，即x=10/2
  x%=2;     //1  x=x%2，即x=5%2

  加号 + 也可以将字符串想加,javascript在处理不同类型的数据时，自动进行类型的统一转换
6.比较(符合判断条件时，返回值是 true ;否则，返回值是 false)
  x == y  //等于 (判断x，y的值是否相等)
  x === y //等于 (判断x，y的值和类型是否都相同)
  x != y  //不等于 (判断x，y的值是否不相等)
  x > y   //大于 (判断x是否大于y)
  x < y   //小于 (判断x是否小于y)
  x >= y  //大于等于 (判断x是否大于或者等于y)
  x <= y  //小于等于 (判断x是否小于或者等于y)
7.逻辑
  &&  (与)  
  || (或) 
  ! (非)
8.条件
  condition ? expr1 : expr2
  条件 condition 实际值为true时，执行expr1
       condition 实际值为false时，执行expr2
  
